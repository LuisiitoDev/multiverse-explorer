name: CI Rick And Morty
run-name: CI Rick And Morty
on: 
    workflow_dispatch:

jobs:
    build:
        runs-on: ubutu-latest
        steps:
            - uses: actions/checkout@v4

            name: Set up Node.js
            uses: actions/setup-node@v3
            with:
                node-version: '20'
