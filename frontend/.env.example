# Feature flags. Accepted values: true/1/on/enabled, false/0/off/disabled.
# Unset or unrecognised values fall back to DEFAULT_FEATURE_FLAGS.
#
# Vite inlines these at BUILD time, so changing one requires a rebuild --
# setting it on an already-built container has no effect.
VITE_FEATURE_CHARACTER_MODAL_V2=false
